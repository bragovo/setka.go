name: Build and Deploy

on: push

# defaults:
#   run:
#     working-directory: ./k8s

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: azure/k8s-set-context@v2
      with:
        token: ${{ secrets.TOKEN }}
